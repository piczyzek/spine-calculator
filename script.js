$( document ).ready(function() {
      var thickness = $( "#paper_type" ).val();


    /**var volume = input/2;
    var paperboard = input2;
  if (hardcover = selected){ 
    document.getElementById("hrdcvr").removeAttribute("twarda");
      if (round = selected){
          console.log((thickness * volume + paperboard)* 1.1);}
      if else (straight = selected){
          console.log(thickness * volume + paperboard);}
      else {
          alert("Choose spine style!");}
      }
  else {
    console.log(thickness * volume);
  }**/
  
});
